# 보안 정책

AI Office는 로컬 작업 공간, 에이전트 권한, 모델 API 자격 증명을 다룹니다.

## 보고

취약점을 공개 이슈로 등록하지 말고 저장소 관리자에게 비공개로 전달합니다. 보고에는 재현 절차, 영향 범위, 영향을 받는 파일 또는 엔드포인트를 포함해 주세요.

## 운영 원칙

- `OPENROUTER_API_KEY`, `OPENAI_API_KEY`, MCP 토큰을 소스 코드와 로그에 남기지 않습니다.
- 에이전트 작업은 TaskContract의 허용 경로와 명령 범위 안에서만 실행합니다.
- 로컬 데이터베이스와 작업 공간은 운영 산출물과 분리합니다.
- 외부 연동은 필요한 경우에만 연결하고 최소 권한을 사용합니다.

